# Supabase Configuration
# 
# یہ file کو copy کریں اور .env.local نام سے save کریں
# Copy this file as .env.local and fill in your credentials
#
# Supabase Dashboard سے یہ values حاصل کریں:
# Get these values from Supabase Dashboard:
# Settings > API

# Your Supabase Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase Anonymous (public) Key
# یہ key frontend میں استعمال ہوگی - یہ safe ہے public کرنا
# This key will be used in frontend - it's safe to expose
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ⚠️ Service Role Key (Backend only - never expose in frontend!)
# یہ key صرف backend میں استعمال کریں - frontend میں نہیں!
# VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Email Service Configuration (Optional)
# اگر email notifications چاہیں تو یہ add کریں
# Add these if you want email notifications

# SendGrid (Option 1)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Resend (Option 2)
# RESEND_API_KEY=your-resend-api-key

# Admin Email
# یہاں وہ email ڈالیں جہاں notifications آنی چاہیں
# Add email address where you want to receive notifications
VITE_ADMIN_EMAIL=your-email@example.com

# ⚠️ Important Notes:
# 1. اس file کو Git میں commit نہ کریں / Don't commit this file to Git
# 2. .gitignore میں .env.local add ہونا چاہیے / Should be in .gitignore
# 3. Vercel/deployment platform میں manually add کریں / Add manually to deployment platform
# 4. Development کے لیے npm run dev restart کریں / Restart dev server after changes

